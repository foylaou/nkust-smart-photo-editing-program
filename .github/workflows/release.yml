name: Manual Release with Backend

on:
  workflow_dispatch:

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- Backend Build Steps ---
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        working-directory: ./backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build backend executable
        working-directory: ./backend
        run: pyinstaller --name ipc_server --onefile --noconsole ipc_server.py

      # --- Frontend Build Steps ---
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Node.js dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build frontend and package application
        working-directory: ./frontend
        run: npm run build:all

      # --- Release Steps ---
      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ fromJson(file('frontend/package.json')).version }}
          name: Release v${{ fromJson(file('frontend/package.json')).version }}
          body: |
            New release v${{ fromJson(file('frontend/package.json')).version }}
            Includes updated frontend and backend.
          draft: false
          prerelease: false
          files: |
            frontend/release/*
